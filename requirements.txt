# ---------------------------------------------------------------------------
# CardioSense — Phase 1 requirements (local / full install)
#
# Version policy:
#   Lower bounds are pinned to the first release whose API this codebase uses.
#   Upper bounds are only used where a known breaking change exists.
#   On Google Colab, use requirements-colab.txt instead (torch/torchvision are
#   preinstalled there and must NOT be reinstalled — it breaks the CUDA build).
# ---------------------------------------------------------------------------

# --- Core scientific stack -------------------------------------------------
numpy>=1.26,<3.0
pandas>=2.1
scipy>=1.11
pyyaml>=6.0
joblib>=1.3
tqdm>=4.66

# --- Classical ML ----------------------------------------------------------
# scikit-learn >=1.6 deprecated CalibratedClassifierCV(cv="prefit") in favour of
# sklearn.frozen.FrozenEstimator. src/cardiosense/common/compat.py handles both,
# so any version in this range works.
scikit-learn>=1.4,<1.9
xgboost>=2.0

# --- Explainability (clinical) --------------------------------------------
shap>=0.45

# --- Plotting --------------------------------------------------------------
matplotlib>=3.8
seaborn>=0.13

# --- Dataset access --------------------------------------------------------
ucimlrepo>=0.0.7        # UCI Heart Disease (clinical)
wfdb>=4.1               # PTB-XL waveform reading (ECG)
kaggle>=1.6             # NIH ChestX-ray14 download (X-ray)

# --- Deep learning ---------------------------------------------------------
torch>=2.2
torchvision>=0.17
captum>=0.7             # Integrated Gradients for the 1D-CNN (ECG)

# --- Imaging ---------------------------------------------------------------
pillow>=10.2
opencv-python-headless>=4.9   # Grad-CAM colour maps / overlays
scikit-image>=0.22            # simple image features for the X-ray baseline

# --- Dev / notebooks -------------------------------------------------------
jupyter>=1.0
ipykernel>=6.29
tabulate>=0.9
pytest>=8.0
