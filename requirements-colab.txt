# ---------------------------------------------------------------------------
# CardioSense — Phase 1 requirements for Google Colab
#
# Colab already ships a CUDA-enabled torch/torchvision matched to its driver.
# Reinstalling them is the single most common cause of "CUDA not available"
# after a runtime restart, so they are deliberately absent from this file.
#
# Usage inside a Colab cell:
#     !pip install -q -r /content/CardioSense/requirements-colab.txt
# ---------------------------------------------------------------------------

pyyaml>=6.0
xgboost>=2.0
shap>=0.45
ucimlrepo>=0.0.7
wfdb>=4.1
captum>=0.7
scikit-learn>=1.4,<1.9
seaborn>=0.13
tabulate>=0.9

# numpy, pandas, scipy, matplotlib, pillow, opencv, scikit-image, joblib, tqdm
# and kaggle are preinstalled on Colab. If a fresh Colab image ever drops one,
# add it here rather than to requirements.txt.
